# Frame

## Product plan for a Shopify Liquid theme build tool

**Working name:** Frame

**Product family:** Blueprint

**Proposed positioning:** Blueprint defines the theme architecture; Frame turns its source into production-ready Shopify assets.

> Frame is a Shopify-aware Vite adapter and asset development server. It compiles frontend source into a safe, deterministic Shopify theme asset graph while Shopify CLI remains an independent, first-class tool for theme development, synchronization, previews, deployment, and packaging.

## Executive summary

Frame should be built as a focused Vite plugin with a small optional CLI, not as a new JavaScript bundler, starter theme, storefront runtime, or Shopify CLI replacement.

The Shopify theme tooling ecosystem has several recurring problems:

- Vite and Shopify CLI usually run as independent processes with poorly coordinated reload behavior.
- Existing plugins silently take ownership of substantial Vite configuration.
- Development and production both need a generated Liquid snippet, creating mutable generated state inside the theme.
- Generated files share `assets/` with hand-authored files, making cleanup dangerous.
- Glob-based entry discovery can accidentally create hundreds of entries and oversized Liquid snippets.
- CORS, local HTTPS, tunnels, Theme Editor iframes, and dynamic ports create recurring setup failures.
- Liquid changes can reload before Shopify CLI finishes uploading them, producing stale or “one save behind” pages.
- Broad tools duplicate Shopify CLI synchronization and inherit a large API, cache, authentication, and race-condition surface.
- Starters solve onboarding but couple the build pipeline to a CSS framework, JavaScript framework, and specific theme architecture.

Frame’s opportunity is to provide a narrow and dependable integration layer:

1. Explicit multi-entry Vite builds.
2. Stable entry assets with content-hashed shared and dynamic chunks.
3. Compact generated Liquid based on a versioned output contract.
4. Safe generated-file ownership and stale cleanup.
5. Optional reload coordination through Shopify CLI’s public `--notify` mechanism without Frame launching or wrapping Shopify CLI.
6. Diagnostics that explain exactly what Frame resolved and owns.
7. No mandatory storefront runtime, framework, starter, or custom source layout.

## Confirmed technical decisions

### Shopify CLI remains independently runnable

Frame will not launch, wrap, replace, or proxy Shopify CLI. Users retain the normal Shopify CLI commands and can run them directly alongside Frame.

The intended workflows are:

- Development terminal one: `npm run dev` runs Vite with the Frame plugin.
- Development terminal two: `shopify theme dev` runs Shopify’s theme synchronization and preview server.
- Build and deployment: `npm run build` runs `vite build` with the Frame plugin, after which the user runs `shopify theme push`, `shopify theme package`, or another Shopify CLI command directly.
- Project-level package scripts may compose these commands, but the Frame package itself will not spawn Shopify CLI.

Frame will not implement custom `frame dev` or `frame build` commands. Vite remains the development server and build command. Future Frame-specific commands are reserved for capabilities Vite does not provide, such as `frame inspect`, `frame doctor`, `frame clean`, and `frame restore`.

A built-in notification handshake is enabled by default at `.frame/shopify-ready`. The user passes that path to `shopify theme dev --notify`, and Frame observes the signal to reload after Shopify CLI finishes processing Liquid changes. The Frame watcher can be disabled or assigned another signal path and debounce delay. The workflow must also function without this handshake by relying on Shopify CLI’s normal live reload.

This boundary ensures that every Shopify CLI option, environment, authentication method, and future CLI release remains available without Frame needing to mirror or forward arguments.

### Generated Liquid is ignored build output

`snippets/frame-assets.liquid` is generated by Frame and ignored by Git by default. The source-controlled layout contains the stable `{% render 'frame-assets' %}` integration point.

- `npm run dev` writes the development form with the Vite client, development origin, and source entries.
- `npm run build` writes and verifies the production form with Shopify `asset_url` references.
- Users must run `npm run build` before `shopify theme push` or `shopify theme package`.
- Frame records the last successful production form under `.frame/` and restores it after normal development shutdown where practical.
- `frame doctor` and `frame restore` are planned recovery tools for interrupted sessions.
- Shopify GitHub integration requires a compiled deployment branch or repository because it does not execute the Vite build.

This removes normal dev/build changes from the Git working tree without attempting unreliable runtime environment detection in Liquid.

### Source layout and entries are user-configurable

Frame does not prescribe a `src` directory. Users configure a source directory and an explicit named entry map. Entry paths resolve relative to the configured source directory, while the source directory resolves relative to Vite’s root. The Shopify theme defaults to the repository root and can optionally be in a nested directory through `theme`.

Frame does not create implicit `@` or `~` aliases. Users retain native Vite aliases. Frame only reserves its documented output namespace inside the theme.

### Volt companion-plugin capabilities

Frame treats Volt’s three companion-plugin concerns differently:

- **Asset cleanup is core.** Frame stages builds and records a theme-specific ownership ledger. It removes only stale Frame-owned files after a successful replacement build and never empties the shared Shopify `assets/` directory.
- **Page reload is a built-in development integration.** Vite HMR handles JavaScript and CSS in Frame’s module graph. Shopify CLI handles Liquid and standard theme-file uploads. Frame observes the file passed to `shopify theme dev --notify` and requests one full reload only after Shopify CLI reports idle; it does not add a competing broad Liquid-file watcher.
- **Import maps are optional.** Standard Frame builds bundle bare imports and do not require browser import maps. Frame’s versioned manifest should make a separate import-map integration possible later without making import maps, shims, or a storefront loader part of the core runtime.

## Product principles

### 1. Vite-native

Frame should use Vite and its normal plugin ecosystem for compilation. It should not reproduce module resolution, dependency graphs, HMR, CSS processing, static asset handling, code splitting, or framework integrations.

### 2. Shopify CLI-native

Shopify CLI should remain responsible for:

- Authentication.
- Development themes.
- Theme preview and Theme Editor URLs.
- Uploading Liquid, JSON, locale, config, and template changes.
- Pulling themes.
- Pushing themes.
- Packaging themes.
- Theme Editor synchronization.

Frame must not launch or wrap `shopify theme dev`. The user runs Shopify CLI independently alongside Frame. Frame provides a documented file-based handshake using Shopify CLI’s public `--notify` option.

### 3. Safe by construction

Frame must never broadly empty the Shopify `assets/` directory. It should only modify or remove files it can prove it generated.

### 4. Explicit over magical

Named entrypoints should be explicit. Automatic discovery can be added as an optional convenience, but users must be able to inspect the resulting entry graph.

### 5. Observable

Frame should explain:

- Which theme it found.
- Which entries it resolved.
- Which Vite settings it added.
- Which files it generated.
- Which files it considers stale.
- Which development origin it selected.
- Why a reload or rebuild occurred.

### 6. Incrementally adoptable

A developer should be able to add Frame to Dawn, Skeleton, an existing agency theme, or a commercial theme without moving the theme into a prescribed source tree.

### 7. No storefront runtime by default

A theme built with Frame should not require a Frame browser runtime in production. Islands, import maps, and component runtimes can be optional integrations built on Frame’s manifest contract.

## Scope

### Frame should own

- Vite-to-Shopify theme configuration.
- Explicit entrypoint resolution.
- Production bundle metadata.
- Generated Liquid asset tags.
- Safe copying of generated files into `assets/`.
- Generated-file ownership and cleanup.
- Vite development-server origin handling.
- Vite HMR for JavaScript and CSS.
- Coordinating browser reloads with completed Shopify CLI theme updates.
- Diagnostics and production-output verification.

### Frame should not own

- Shopify authentication.
- Direct Theme API uploads.
- Pulling or reconciling remote themes.
- Metafield, metaobject, file, page, redirect, or product synchronization.
- A starter theme.
- A mandatory directory structure outside Shopify’s standard theme structure.
- Tailwind, Sass, PostCSS, React, Vue, Alpine, Svelte, or another UI framework.
- An islands runtime.
- Import-map behavior.
- Liquid source transformations.
- Section-folder compilation in the initial release.
- Git branch policy.
- Git `skip-worktree` manipulation.
- Automatic editing of user-owned Liquid files.
- Deployment policy beyond validating Frame’s generated output.

## Ecosystem map

The products commonly compared in this space actually belong to different categories.

| Category | Examples | What they provide |
| --- | --- | --- |
| Focused Vite adapters | Barrel Shopify Vite, `woodawn/vite-plugin-shopify-theme` | Translate Vite output and development behavior into Shopify theme conventions |
| Theme-aware compilers and synchronizers | Syncify, Melter | Transform, watch, map, and sometimes upload broad portions of a theme |
| Development environments and starters | Theme Lab, Adastra, Slayed, Shopify Theme Tailwind | A complete source layout, framework stack, scripts, and example theme |
| Older all-in-one pipelines | Slatest, Slate, Theme Kit wrappers | Webpack, Gulp, BrowserSync, Sass, and legacy upload workflows |
| Focused extensions | Import-map, islands, cleanup, Ajax cart packages | Add one specialized capability around the core workflow |
| Official foundation | Shopify CLI, Theme Check, Liquid language tooling | Preview, sync, deploy, validation, formatting, and editor intelligence |

Frame should compete with focused Vite adapters. It should use starters as fixtures and leave specialized behavior to extensions.

## Competitive analysis

The repository and package observations below were gathered on September 3, 2026. Popularity and activity figures are snapshots rather than permanent measures.

### Barrel Shopify Vite

- Documentation: <https://shopify-vite.barrelny.com/>
- Repository: <https://github.com/barrel/shopify-vite>
- Package: <https://www.npmjs.com/package/vite-plugin-shopify>
- Snapshot: 459 GitHub stars and approximately 14,466 weekly npm downloads.
- Current inspected version: 5.0.0.

#### Architecture

Barrel provides a set of Vite plugins that:

- Discover entries with filesystem globs.
- Configure Vite’s root-relative base and Shopify `assets/` output.
- Disable Vite’s public directory by default.
- Generate a Vite manifest.
- Generate `snippets/vite-tag.liquid` for development and production.
- Configure aliases, HMR, CORS, allowed hosts, tunnels, and React Refresh.
- Add a page-reload plugin for non-module theme files.

#### What is good

- It keeps Vite as the compiler.
- It is the most established purpose-built Vite integration for Shopify Liquid themes.
- It is actively maintained and supports current Vite majors.
- It presents normal Vite concepts in Shopify theme terms.
- It supports multiple JavaScript and CSS entrypoints.
- It handles CSS emitted from JavaScript entries.
- It generates module-preload tags for static imports.
- It supports development assets served by Vite and production assets served by Shopify’s CDN.
- It includes React Refresh support without requiring React.
- It has Linux, macOS, and Windows CI.
- It has generated-Liquid ergonomics that theme developers can use without understanding Vite’s manifest directly.

#### What is bad or risky

- It takes ownership of substantial Vite configuration, including `rollupOptions.input`, output paths, manifest behavior, aliases, public directories, and server settings.
- Glob discovery can make every file under an entry directory a separate entry.
- The generated Liquid snippet grows linearly with the entry count.
- Large generated snippets have exceeded Shopify’s 256 KB template limit.
- The development and production forms of the same generated snippet are mutable state inside the theme.
- Asset cleanup has historically required a separate package and repeated compatibility work.
- The Vite and Shopify development processes are not inherently coordinated.
- Broad CORS and tunnel handling introduces operational complexity into the core plugin.
- Users can create unsupported states by changing Vite’s manifest or output configuration.
- Static assets, imported fonts, CSS ordering, Theme Editor execution, and cache semantics have generated recurring support issues.

#### Notable issue evidence

- Oversized generated Liquid: <https://github.com/barrel/shopify-vite/issues/100> and <https://github.com/barrel/shopify-vite/issues/230>
- Theme changes one save behind: <https://github.com/barrel/shopify-vite/issues/137>
- Running Vite and Shopify together: <https://github.com/barrel/shopify-vite/issues/119>
- Cleanup behavior: <https://github.com/barrel/shopify-vite/issues/120> and <https://github.com/barrel/shopify-vite/issues/219>
- Vite/Theme Editor CORS: <https://github.com/barrel/shopify-vite/issues/165>, <https://github.com/barrel/shopify-vite/issues/188>, and <https://github.com/barrel/shopify-vite/issues/204>
- Theme Editor double execution: <https://github.com/barrel/shopify-vite/issues/169>
- CSS ordering: <https://github.com/barrel/shopify-vite/issues/93>
- Fonts and static assets: <https://github.com/barrel/shopify-vite/issues/166>, <https://github.com/barrel/shopify-vite/issues/221>, and <https://github.com/barrel/shopify-vite/issues/23>
- Windows paths: <https://github.com/barrel/shopify-vite/issues/35>
- CDN cache and version behavior: <https://github.com/barrel/shopify-vite/issues/25>, <https://github.com/barrel/shopify-vite/issues/36>, and <https://github.com/barrel/shopify-vite/issues/83>

#### What Frame should copy

- Thin Vite adapter positioning.
- Generated Liquid helpers.
- Multi-entry support.
- Entry CSS and static-import handling.
- Vite-native framework composition.
- Current-Vite compatibility and cross-platform CI.
- Clear migration into an ordinary Shopify theme.

#### What Frame should improve

- Require explicit entry maps in the initial API.
- Detect configuration conflicts instead of silently replacing user settings.
- Integrate safe cleanup directly.
- Coordinate the development processes through Shopify CLI notifications.
- Keep generated Liquid compact and version its format.
- Expose the resolved configuration and generated-file graph.

### `woodawn/vite-plugin-shopify-theme`

- Repository: <https://github.com/woodawn/vite-plugin-shopify-theme>
- Package: <https://www.npmjs.com/package/vite-plugin-shopify-theme>
- Snapshot: 2 GitHub stars and approximately 55 weekly npm downloads.
- Current inspected version: 0.1.0.

#### Architecture

This package combines a focused Vite adapter with a companion CLI. It includes:

- A theme runtime and validated theme target.
- A primary entry built into Shopify’s `assets/`.
- Stable entry filenames and content-hashed shared chunks.
- A generated “mixer” Liquid snippet.
- Stale-output pruning constrained to a reserved namespace.
- Theme-target locking.
- Git working-tree handling.
- Development reload policy.
- Shopify CLI process coordination.
- `doctor`, production verification, push, package, restore, and development commands.

#### What is good

- Strong test density for a new package: approximately 66 test cases in the inspected repository.
- Linux, macOS, and Windows CI across Node 22 and Node 24.
- Stable top-level entry filenames reduce generated Liquid churn.
- Shared and dynamic chunks remain content-addressed.
- Cleanup is restricted to a reserved filename namespace.
- Includes a useful diagnostics model.
- Detects development artifacts, unresolved aliases, and selected unapproved CDN URLs before production.
- Coordinates Vite and Shopify CLI under a single process lifecycle.
- Includes locking and restoration concepts.
- Adds no required storefront runtime.

#### What is bad or risky

- The current design centers on a single entry.
- It has almost no adoption evidence.
- It includes several policy decisions that do not belong in a generic build adapter:
  - Required development branch prefixes.
  - Git `skip-worktree` behavior.
  - Automatic edits to `layout/theme.liquid`.
  - Warnings based on unrelated Shopify development processes.
  - Push and package wrappers in the same product surface.
- `skip-worktree` can hide real file changes from developers.
- Automatically modifying source Liquid makes adoption less trustworthy.
- Its direct bundle-metadata approach can couple it to Vite-specific internals.
- Locking, Git restoration, deployment checks, process discovery, and CLI delegation create a large operational implementation relative to the plugin’s core responsibility.

#### What Frame should copy

- Stable entries plus hashed chunks.
- Generated-output ownership.
- Production verification.
- An inspect/doctor command.
- Staging, locking, and restoration principles.
- Strong cross-platform integration testing.

#### What Frame should avoid

- Hidden Git state.
- Branch conventions.
- Automatic source-file mutation.
- Owning deployment policy.
- Single-entry assumptions.

### Syncify

- Repository: <https://github.com/panoply/syncify>
- Package: <https://www.npmjs.com/package/@syncify/cli>
- Snapshot: 148 GitHub stars and approximately 35 weekly npm downloads.
- Package state: unstable/beta.

#### Architecture

Syncify is a theme-aware compiler and synchronization environment. It supports:

- Custom path mapping.
- Watch, upload, import, and export workflows.
- Hot reloading.
- Shared section schema behavior.
- Metafield/resource concepts.
- PostCSS, Sass, minification, SVG sprites, images, and broad file transforms.
- A custom CLI and local development server.

#### What is good

- Treats a theme as structured Shopify content rather than anonymous static files.
- Supports nonstandard source directories and explicit target mappings.
- Separates transformation concepts into stages.
- Demonstrates demand for section colocation and reusable schema.
- Provides typed configuration and include/exclude concepts.
- Attempts to unify the theme development loop.

#### What is bad or risky

- Reimplements synchronization that Shopify CLI already owns.
- Must handle authentication, remote deletes, conflicts, API throttling, unpublished changes, caches, and reconciliation.
- Has a large implementation and dependency surface.
- The inspected repository had approximately 217 source files and no visible test suite.
- Open issues show recurring cache, race, deletion, installation, and wrong-project problems.
- Transforming Liquid and JSON means local source can differ substantially from the uploaded theme.
- Direct API and resource integrations expand the support burden beyond building assets.

#### Notable issue evidence

- Deletion crash: <https://github.com/panoply/syncify/issues/50>
- Wrong-project hot pull: <https://github.com/panoply/syncify/issues/49>
- Cache design: <https://github.com/panoply/syncify/issues/48> and <https://github.com/panoply/syncify/issues/47>
- Installation failure: <https://github.com/panoply/syncify/issues/46>
- Generated `dist/` conflicts: <https://github.com/panoply/syncify/issues/43>
- Upload race producing 422 responses: <https://github.com/panoply/syncify/issues/41>

#### Frame lesson

Copy the explicit file-pipeline and diagnostics ideas. Do not copy the remote synchronization responsibility.

### Melter

- Repository: <https://github.com/unshopable/melter>
- Package: <https://www.npmjs.com/package/@unshopable/melter>
- Snapshot: 18 GitHub stars and approximately 13 weekly npm downloads.
- Current inspected state: alpha and last meaningfully active in 2023.

#### Architecture

Melter implements a custom compiler with:

- Assets and target paths.
- Add, update, and remove actions.
- A compilation object.
- A watcher based on Chokidar.
- An emitter.
- Tapable lifecycle hooks.
- Plugins for path and statistics behavior.

#### What is good

- Clear source-to-target asset model.
- Explicit add/update/remove events.
- A plugin model for custom transformations.
- Acknowledges that theme authors may want a custom source tree compiled into Shopify’s required structure.

#### What is bad or risky

- Reimplements module- and asset-pipeline behavior available in Vite.
- Plugins must target Melter’s custom hooks rather than the Vite ecosystem.
- No visible tests in the inspected repository.
- Alpha maturity and very low adoption.
- Current source contains asynchronous correctness risks where compilation and emission work is initiated without consistently being awaited.
- Custom watchers, emitters, and path transforms increase cross-platform and incremental-build risk.

#### Frame lesson

Use the source/asset/target mental model, but express it through Vite’s plugin lifecycle and a public Frame manifest.

### Shopify Theme Lab

- Repository: <https://github.com/uicrooks/shopify-theme-lab>
- Snapshot: 822 GitHub stars; last meaningful source push in February 2024.

#### What is good

- Strong one-command onboarding.
- Complete working theme example.
- Clear source and generated-theme separation.
- Demonstrates component-framework and Tailwind integration.
- Provides linting, formatting, development, and deployment scripts.

#### What is bad or risky

- Uses Webpack rather than Vite.
- Couples Vue, Vuex, Tailwind, Babel, linting, style tooling, and theme architecture.
- Broad dependencies must be upgraded together.
- Forking a starter is not a clean package-upgrade path.
- Open Shopify CLI compatibility issues have remained unresolved.
- Generated and source theme states can be difficult to reconcile with Theme Editor changes.

#### Frame lesson

Copy onboarding quality and examples. Do not make a starter repository the product or upgrade mechanism.

### Adastra

- Repository: <https://github.com/odestry/adastra>
- Snapshot: 289 GitHub stars.
- Status: archived and explicitly no longer maintained.

#### What is good

- Coherent modern-theme workflow.
- Vite-based development.
- CLI and scaffolding concepts.
- Multiple framework examples.
- Real theme fixtures rather than only synthetic plugin tests.
- Good demonstration of how build ergonomics affect section and snippet authoring.

#### What is bad or risky

- Coupled CLI, plugin, starter templates, branding, formatting, framework examples, and theme architecture.
- Broad package family increased maintenance cost.
- Theme forks are difficult to upgrade.
- Its own issue history includes Theme Editor glitches, first-run failures, Windows problems, and build cleanup deleting fonts.

#### Frame lesson

Use complete themes as integration fixtures but keep the Frame package independent of them.

### Slayed

- Repository: <https://github.com/james0r/slayed>
- Snapshot: 130 GitHub stars.

#### What is good

- Practical combination of Shopify CLI, Vite, Tailwind, Alpine, Liquid Ajax Cart, and Shopify Vite.
- Defines clear development, build, staging, and production scripts.
- Uses `shopify.theme.toml` environments.
- Uses `shopify theme dev --notify`, providing a strong model for reliable reload coordination.
- Shows a realistic agency theme rather than an isolated plugin.

#### What is bad or risky

- It is consumed as a theme starter rather than an independently upgradeable build package.
- Framework and store implementation decisions are mixed with workflow decisions.
- The core build behavior still comes from Barrel Shopify Vite.

#### Frame lesson

The `--notify` integration is one of the most valuable ideas in the ecosystem. Treat Slayed as an end-to-end fixture and recipe.

### Shopify Theme Tailwind

- Repository: <https://github.com/wesselvanree/shopify-theme-tailwind>
- Snapshot: 165 GitHub stars; last source push in April 2024.

#### What is good

- Straightforward Vite and Tailwind integration.
- Includes Liquid files in Tailwind’s source discovery.
- Demonstrates a source theme and generated distribution branch.
- Provides a GitHub Actions distribution model.

#### What is bad or risky

- Framework-specific.
- Sensitive to Tailwind major-version configuration changes.
- Requires developers to understand static class extraction and safelisting.
- Dynamic Liquid-generated class names remain impossible to infer reliably.

#### Frame lesson

Document Tailwind as a recipe and integration test. Do not include it in Frame core.

### Trellis Dawn and Tailwind starter

- Repository: <https://github.com/TrellisCommerce/shopify-tailwind-starter-base>
- Snapshot: 388 GitHub stars and 449 forks.

#### What is good

- Active Dawn-based reference.
- Tailwind and Prettier integration.
- Theme Check and Lighthouse-oriented CI.
- Real agency workflow and adoption signals.

#### What is bad or risky

- It is a maintained Dawn fork, not a reusable bundler abstraction.
- Dawn upgrades and Tailwind changes must be merged into the starter.
- Theme architecture and build-tool evaluation are mixed together.

#### Frame lesson

Use it to validate that Frame works with a large Dawn-derived theme and existing CI conventions.

### Slatest

- Repository: <https://github.com/entozoon/slatest>
- Package: <https://www.npmjs.com/package/slatest>
- Snapshot: 23 GitHub stars and approximately 50 weekly downloads.

#### What is good

- Shows the complete set of needs older theme pipelines attempted to solve.
- Includes watch, BrowserSync, Sass, Babel, CSS extraction, and multiple JavaScript framework loaders.

#### What is bad or risky

- Webpack-era architecture.
- Large hard dependency list.
- Reimplements behavior modern Vite provides directly.
- Broad framework support makes the core difficult to update and test.

#### Frame lesson

Do not create another all-in-one frontend stack.

### `shopify-theme-dev` and `shopify-accelerate`

- Repositories: <https://github.com/FelixTellmann/shopify-theme-dev> and <https://github.com/FelixTellmann/shopify-accelerate>
- Packages: <https://www.npmjs.com/package/shopify-theme-dev> and <https://www.npmjs.com/package/shopify-accelerate>
- Snapshot: `shopify-theme-dev` had approximately 15 weekly downloads; `shopify-accelerate` approximately 759.

#### What is good

- Demonstrate interest in TypeScript-first theme development.
- Include schema/type generation and structured build ideas.
- Treat optimization and authoring conveniences as build-time capabilities.

#### What is bad or risky

- Large dependency surfaces spanning SWC, esbuild, Shopify APIs, Tailwind, PostCSS, state management, schema generation, JSDOM, and CLI behavior.
- Product boundary is difficult to understand.
- Low public repository adoption despite package downloads.
- Direct Shopify API dependencies expand maintenance and security responsibility.

#### Frame lesson

Typed helpers and advanced optimization should be separate packages or recipes built after the core contract stabilizes.

### Import maps

- Representative package: <https://github.com/slavafyi/vite-plugin-shopify-import-maps>
- Snapshot: 21 GitHub stars and approximately 467 weekly downloads.

#### What is good

- Supports stable browser module specifiers.
- Can decouple source imports from content-hashed outputs.
- Builds on top of Shopify Vite instead of replacing it.

#### What is bad or risky

- Requires import-map ordering before module execution.
- Often requires `es-module-shims` for compatibility.
- Makes CSP, module preload, and browser support part of the contract.
- Introduces a runtime loading policy beyond basic bundling.

#### Frame lesson

Publish a stable manifest API that an optional import-map plugin can consume.

### Islands architecture

- Representative package: <https://github.com/Rees1993/vite-plugin-shopify-theme-islands>
- Package: <https://www.npmjs.com/package/vite-plugin-shopify-theme-islands>
- Snapshot: 7 GitHub stars and approximately 131 weekly downloads.

#### What is good

- Encourages section-level or component-level JavaScript instead of a single large global bundle.
- Can reduce JavaScript shipped to pages that do not use a component.
- Shows how a build manifest can support higher-level integrations.

#### What is bad or risky

- Introduces a storefront runtime.
- Must handle Theme Editor section load, unload, reorder, selection, and duplicate instances.
- Must define serialization and hydration behavior.
- Moves beyond a build adapter into an application architecture.

#### Frame lesson

Keep islands optional and build them against Frame’s public manifest and lifecycle documentation.

### Cleanup plugins

- Representative package: <https://www.npmjs.com/package/@driver-digital/vite-plugin-shopify-clean>
- Snapshot: approximately 1,640 weekly downloads.

#### What is good

- Solves a real gap: Vite’s hashed chunks become stale when output goes into a shared Shopify `assets/` directory.

#### What is bad or risky

- Broad globs or `emptyOutDir` can delete merchant-, app-, or developer-owned assets.
- Cleaning before a build succeeds can leave a theme broken.
- Naming conventions alone can conflict with user files unless the namespace is reserved and documented.

#### Frame lesson

Cleanup belongs in core because it is inseparable from the output contract, but it must use an ownership ledger and occur only after a successful replacement build.

## Official Shopify foundation

Frame should compose with the official stack rather than reproduce it.

### Shopify CLI

- Documentation: <https://shopify.dev/docs/storefronts/themes/tools/cli>
- Repository: <https://github.com/Shopify/cli>
- Package: <https://www.npmjs.com/package/@shopify/cli>
- Snapshot: approximately 511,464 weekly npm downloads.

Relevant responsibilities and capabilities:

- `shopify theme dev` creates and updates development themes.
- Shopify CLI uploads theme source changes.
- It provides preview and Theme Editor URLs.
- It supports CSS and section hot reload plus full-page reload policies.
- It supports `shopify.theme.toml` environments.
- It supports `--theme-editor-sync` and reconciliation behavior.
- Its `--notify` option updates a file or posts to a URL after file-change processing becomes idle.

When the user opts into cross-process reload coordination, Frame should observe the destination passed by the user to Shopify CLI’s `--notify` option rather than guessing when a Liquid upload is complete. Frame must not spawn Shopify CLI to establish this handshake.

### Theme Check and Liquid tooling

- Repository: <https://github.com/Shopify/theme-tools>
- Theme Check: <https://www.npmjs.com/package/@shopify/theme-check-node>
- Prettier plugin: <https://www.npmjs.com/package/@shopify/prettier-plugin-liquid>
- Parser: <https://www.npmjs.com/package/@shopify/liquid-html-parser>
- Language server: <https://www.npmjs.com/package/@shopify/theme-language-server-node>

Frame should not build its own Liquid parser, formatter, language server, or theme linter. It may invoke or recommend Theme Check during verification.

### Dawn and Skeleton Theme

- Dawn: <https://github.com/Shopify/dawn>
- Skeleton: <https://github.com/Shopify/skeleton-theme>

Frame should use both as permanent integration fixtures:

- Skeleton for the smallest official theme architecture.
- Dawn for a large, production-oriented theme with realistic assets and sections.

## Recurring pain points and product opportunities

### 1. Development process coordination

Vite and Shopify CLI run as independent tools. Without an explicit handshake, Vite does not know when Shopify CLI has completed a Liquid upload.

Consequences:

- Duplicate browser reloads.
- Reload before Shopify has uploaded Liquid.
- “One save behind” pages.
- Confusion about which tool owns each reload.

**Frame opportunity:** keep the two tools independent while supporting a `--notify` handshake. The user passes Frame’s signal-file path to `shopify theme dev`; Frame observes it and reloads only after Shopify CLI reports idle. Frame never launches Shopify CLI or owns its process lifecycle.

### 2. Safe generated asset cleanup

Vite emits hashed chunks. When an import changes, the old chunk remains in Shopify’s `assets/` unless something removes it.

Consequences:

- Stale assets accumulate.
- Theme packages become larger.
- Broad cleanup can destroy hand-authored assets.

**Frame opportunity:** maintain an atomic generated-file ledger and remove only previously owned files after a successful new build.

### 3. Generated Liquid size

A dispatcher with conditions for every discovered entry can exceed Shopify’s file limit.

**Frame opportunity:** explicit entries, compact names, predictable stable entry filenames, a size check, and a future per-entry snippet strategy if needed.

### 4. Vite configuration ownership

Plugins that silently replace `rollupOptions.input`, output naming, `manifest`, `publicDir`, aliases, or CORS can conflict with an existing application.

**Frame opportunity:** merge only unambiguous settings, detect conflicts, and show a complete resolved configuration through `frame inspect`.

### 5. Theme Editor lifecycle

The Theme Editor can re-render a section without reloading the full page. Global initialization can run twice, while section-specific initialization can fail to run after a replacement.

**Frame opportunity:** keep core runtime-free, but publish a documented lifecycle recipe and optional helper for idempotent initialization and teardown. Do not disguise this as generic Vite HMR.

### 6. CORS, local network access, HTTPS, and tunnels

The theme page is rendered on a Shopify or CLI origin while JavaScript and CSS may come from the Vite origin. Browsers increasingly restrict local-network access from remote pages.

**Frame opportunity:**

- Compute the actual bound Vite origin after the server listens.
- Allow known Shopify storefront and Admin origins narrowly.
- Support explicit local, LAN, and custom external origins.
- Diagnose unreachable origins.
- Defer managed tunnel providers until after the base workflow is stable.

### 7. Static assets and CSS URLs

Imported fonts, images, and CSS `url()` references need output names and relative URLs that survive Shopify’s flat asset CDN.

**Frame opportunity:** include fonts and images in the ownership manifest, test CSS URL rewriting, and reject output paths Shopify cannot serve.

### 8. Cache invalidation

Stripping Shopify asset version query parameters can leave stable entry files stale. Hashing every top-level entry forces Liquid to change on every build.

**Frame opportunity:** use stable top-level entry names with Shopify’s `asset_url` query string and hashed shared/dynamic chunks.

### 9. Source-controlled versus generated themes

Some teams commit deployable assets; others build them in CI. Shopify’s GitHub theme integration does not run a Node build.

**Frame opportunity:** explicitly support and document two workflows:

- **Pipeline mode:** source branch ignores generated output; CI builds a package or distribution branch before Shopify deployment.
- **Repository mode:** production outputs are committed; development temporarily writes tool-owned files and restores production state on exit.

Avoid silently selecting a Git policy.

## Proposed package design

Start with one package, tentatively named `@blueprint/frame`, that exports the Vite plugin and installs a `frame` executable.

Possible later packages:

- `@blueprint/frame-islands`
- `@blueprint/frame-import-maps`
- `@blueprint/frame-sections`
- `@blueprint/frame-tailwind`

Do not create these packages before the core manifest contract is stable and a real user requests them.

## Public API philosophy

Keep the initial Frame-specific configuration small:

- `themeRoot`: Shopify theme path, resolved relative to Vite’s root; defaults to the Vite root.
- `sourceDir`: source-code path, resolved relative to Vite’s root; defaults to `src`.
- `entries`: required explicit map of public entry names to source files; relative entry paths resolve from `sourceDir`.
- `snippet`: generated Liquid snippet filename under the theme’s `snippets/` directory; defaults to `frame-assets.liquid`.
- `outputPrefix`: reserved namespace for Frame-generated assets; defaults to `frame-`.
- `devOrigin`: local, network, or explicit external origin.
- `cleanup`: enabled, disabled, or dry-run.
- `generatedMode`: pipeline or repository workflow.
- `debug`: detailed diagnostics.

Everything else should remain native Vite configuration.

Do not include Frame options for:

- Tailwind.
- Sass.
- PostCSS.
- React or Vue.
- Minifier selection.
- SVG sprites.
- Image optimization.
- Deployment stores or theme IDs.
- Shopify credentials.
- Git branches.
- Section schema compilation.

## Output contract

### Named entries

Users define a small, explicit map such as global theme, product, collection, cart, or account entries. Frame validates that names are unique, filesystem-safe, resolve to files, and do not collide with existing user-owned theme assets.

JavaScript, TypeScript, JSX, TSX, CSS, and any other format supported by the user’s configured Vite plugins can be an entry. Frame does not infer application frameworks from file extensions.

`sourceDir` is a path-resolution convenience, not a required project architecture. It may be renamed or point to a nested workspace location. Frame should allow source inputs outside the theme directory while strictly constraining every generated output to the validated theme and staging roots.

### Stable top-level entries

Top-level entry files should use stable names within Frame’s namespace. Shopify’s `asset_url` should retain its version query for cache invalidation.

### Content-addressed chunks

Shared and dynamically imported chunks should include content hashes. Relative imports must remain valid on Shopify’s CDN, where theme assets are effectively served from one asset namespace.

### CSS

Frame should:

- Associate emitted CSS with its entry.
- Render styles before the corresponding module script.
- Deduplicate shared CSS tags.
- Preserve deterministic CSS ordering.
- Support CSS-only entries.
- Test imported fonts and images.

### Module preloads

Frame should emit module-preload tags for static imports and avoid preloading dynamic imports unless a future explicit policy enables it.

### Internal manifest

Frame should generate a versioned machine-readable manifest containing:

- Manifest schema version.
- Frame version.
- Entry source and public name.
- Entry JavaScript.
- Entry CSS.
- Static imports.
- Dynamic imports.
- Emitted static assets.
- File hashes and sizes.
- Output ownership.

Frame may use Vite’s official manifest internally, but the Frame manifest should be its stable public contract.

Do not upload the internal manifest to Shopify by default.

### Generated Liquid

The generated Liquid should:

- Be clearly marked as generated.
- Have a documented and versioned format.
- Use `asset_url` and Shopify-native tags in production.
- Include the Vite client and correct entry URL in development.
- Avoid output when an unknown entry is requested, or provide an explicit development warning policy.
- Stay below a conservative size threshold.
- Avoid rewriting when its content is unchanged.

The initial release can use one compact explicit-entry dispatcher. If real projects approach the size threshold, add a per-entry snippet strategy later.

## Safe build algorithm

1. Resolve and validate Frame configuration.
2. Validate the Shopify theme target.
3. Resolve explicit entries.
4. Read the previous successful ownership ledger.
5. Build into a temporary staging directory.
6. Read Vite’s manifest and complete bundle metadata.
7. Construct the Frame manifest.
8. Generate production Liquid in staging.
9. Validate entry files, imports, CSS, aliases, paths, and generated Liquid size.
10. Determine the exact set of previously owned stale files.
11. Copy or atomically replace new outputs in the theme.
12. Remove only stale files proven to be owned by the previous ledger.
13. Write the new ownership ledger last.
14. Report a concise build summary.

If any step before the commit phase fails, the previous successful output remains unchanged.

## Development algorithm

1. Resolve and validate the theme and entries.
2. Start Frame’s Vite server and wait for the actual listening address.
3. Resolve the selected local, network, or custom development origin.
4. Generate the development Liquid only after the origin is known.
5. Use Vite HMR for module JavaScript and CSS.
6. Let the independently running Shopify CLI own Liquid, JSON, locale, template, config, and section uploads.
7. By default, let Shopify CLI own reload behavior for theme-file changes.
8. If notification integration is configured, observe the shared `--notify` file, debounce notifications, and send exactly one full browser reload after Shopify CLI reports idle.
9. Ignore Frame’s generated snippet when processing reload notifications to avoid loops.
10. On normal Frame shutdown, restore the last successful production snippet when repository mode requires it.
11. On a Frame crash, leave enough state for `frame doctor` and `frame restore` to recover safely.

Frame does not start, stop, authenticate, configure, or forward arguments to Shopify CLI.

## CLI plan

Vite remains responsible for development and builds through the consuming project’s `npm run dev` and `npm run build` scripts. Frame does not provide `frame dev` or `frame build`.

### `frame inspect`

Reports:

- Theme root.
- Vite root.
- Entries.
- Output paths.
- Snippet path.
- Development origin policy.
- Generated-file ownership.
- Configuration conflicts.

Support a machine-readable JSON form from the start.

### `frame clean`

- Defaults to dry-run.
- Lists stale Frame-owned files.
- Requires an explicit confirmation flag to remove them outside a normal successful build.

### `frame doctor`

Checks:

- Theme structure.
- Vite configuration.
- Shopify CLI availability.
- Entry existence.
- Snippet production/development state.
- Unresolved aliases.
- Development URLs in production assets.
- Missing generated assets.
- Ownership-ledger integrity.
- Reachability of a selected development origin where practical.

### `frame restore`

Restores the last known successful production generated state without manipulating Git’s skip-worktree index.

## Configuration conflict policy

Frame must not silently override a user setting when doing so changes semantics.

### Merge where safe

Examples:

- Add Frame entries alongside an explicitly compatible input map.
- Add narrowly scoped CORS origins to an existing explicit policy when requested.
- Add aliases under a reserved namespace.

### Fail where ambiguous

Examples:

- A different `outDir` that bypasses Frame’s staging and ownership guarantees.
- `emptyOutDir: true` against a shared Shopify `assets/` directory.
- Conflicting entry names.
- Output filename patterns that make the ownership namespace unsafe.
- A user manifest setting that prevents Frame from resolving CSS and imports.

Every conflict message should explain what Frame requires, what the project currently defines, and the supported resolution.

## Theme Editor behavior

Core Frame should not attempt to transform a Liquid theme into a single-page application.

Documentation should explain that Shopify can replace section markup dynamically and emit events including section load, unload, reorder, and block selection events. Application code should be idempotent and should clean up listeners and component instances before remounting.

A future optional helper may:

- Register mount functions by selector or component name.
- Prevent duplicate initialization.
- Run teardown before a section is replaced.
- Remount only inside the affected section.
- Remain independent of React, Vue, Alpine, or another framework.

This helper should not be required to use Frame.

## Security and supply-chain principles

- Minimize runtime dependencies.
- Use npm trusted publishing and provenance.
- Do not launch or shell out to Shopify CLI.
- Restrict CORS and allowed hosts to explicit known origins.
- Treat custom tunnel URLs as untrusted input and validate their scheme and origin.
- Never execute theme-provided configuration as arbitrary shell commands.
- Never follow output paths outside the validated theme or staging roots.
- Protect against path traversal in entry and output names.
- Use atomic writes for manifests, ledgers, and generated Liquid.
- Publish supported Node, Vite, Shopify CLI, and browser versions.

## Testing strategy

### Unit tests

- Option normalization.
- Entry-name validation.
- Manifest conversion.
- CSS ordering and deduplication.
- Static versus dynamic import handling.
- Development-origin resolution.
- CORS-origin generation.
- Liquid generation.
- Liquid size thresholds.
- Ledger diffing.
- Safe path resolution.
- Reload debouncing.
- Shopify CLI notification-file observation without process coupling.

### Fixture builds

Maintain fixtures for:

- Minimal Shopify Skeleton theme.
- Dawn-derived theme.
- Theme nested inside a monorepo.
- Theme root equal to Vite root.
- JavaScript-only entry.
- CSS-only entry.
- JavaScript importing CSS.
- Multiple entries sharing JavaScript and CSS.
- Dynamic imports.
- Imported fonts and images.
- Existing hand-authored assets.
- Tailwind through the official Vite integration.
- React Refresh.
- Vue or another framework plugin.

### Lifecycle tests

- First build.
- Identical repeated build.
- Incremental source change.
- Entry addition.
- Entry deletion.
- Entry rename.
- Shared chunk replacement.
- Build failure after a successful build.
- Interrupted build.
- Concurrent build attempt.
- Development startup and normal shutdown.
- Development crash and restore.
- Shopify CLI notification bursts.

### Cross-platform tests

CI should include:

- Linux.
- macOS.
- Windows.
- Node 22.
- Node 24.
- Current Vite major.
- Previous supported Vite major.

Specific tests should cover:

- Windows path separators.
- Spaces in repository paths.
- Symlinked monorepo packages.
- Case sensitivity differences.
- Dynamic ports.
- IPv4 and IPv6 listening addresses.

### End-to-end tests

Where credentials and a test store are available, test:

- Development preview outside the Theme Editor.
- Theme Editor preview.
- JavaScript HMR.
- CSS HMR.
- Liquid update after Shopify CLI notification.
- One browser reload per completed update group.
- Section replacement and application reinitialization.
- Production push or package generated by Shopify CLI using Frame’s output.

## Non-negotiable safety invariants

- Frame never deletes a file absent from its previous successful ownership ledger.
- Frame never cleans old output before a replacement build succeeds.
- A failed build leaves the previous production output usable.
- Frame never emits `/@vite/client` into production Liquid.
- Frame never emits unresolved local aliases into production JavaScript.
- Frame never edits user-owned Liquid outside the explicitly configured generated file.
- Frame never silently empties Shopify’s `assets/` directory.
- Repeated identical builds produce identical output.
- Unchanged generated snippets are not rewritten.
- Development startup and shutdown do not depend on hidden Git index state.

## Delivery plan

### Phase 0: competitive bake-off and API validation

**Duration:** 1–2 weeks

Build one Dawn-derived fixture with:

- Barrel Shopify Vite.
- `woodawn/vite-plugin-shopify-theme`.
- A minimal Frame prototype.

Test:

- JavaScript-only and CSS-only entries.
- JavaScript importing CSS.
- Shared chunks.
- Dynamic imports.
- Fonts and images.
- Entry rename and deletion.
- Failed builds.
- Theme Editor behavior.
- Windows paths.
- Custom ports.
- Shopify CLI environments.

Record:

- Installation steps.
- Cold build time.
- Incremental build time.
- Number of emitted files.
- Number of uploaded files.
- Browser reload count.
- Stale output after changes.
- Git working-tree changes.
- Generated Liquid size.

Interview at least 5–10 active Shopify theme developers before freezing the API.

### Phase 1: deterministic production builds

**Duration:** approximately 2 weeks

Deliver:

- Explicit multi-entry API.
- Vite configuration adapter.
- Stable entries and hashed chunks.
- Internal Vite manifest handling.
- Public Frame manifest.
- Generated production Liquid.
- CSS-only and JS-plus-CSS entry support.
- Static import preloads.
- Staged output.
- Ownership ledger.
- Safe stale cleanup.
- Configuration conflict validation.

The phase is complete when `vite build` works safely without the Frame CLI.

### Phase 2: composable development

**Duration:** approximately 2 weeks

Deliver:

- Development Liquid generation.
- Bound Vite-origin discovery.
- Narrow Shopify storefront and Admin origin handling.
- Vite HMR for JavaScript and CSS.
- Built-in `shopify theme dev --notify` signal-file observation.
- One reload after each completed Shopify CLI update group when the handshake is enabled.
- Clear two-terminal documentation for running Frame and Shopify CLI independently.
- Compatibility with ordinary Shopify CLI live reload when the handshake is disabled.
- Local, LAN, and explicit external origins.

Do not include a managed tunnel provider initially.

### Phase 3: diagnostics and recovery

**Duration:** 1–2 weeks

Deliver:

- `frame inspect` with text and JSON output.
- `frame doctor`.
- `frame clean --dry-run`.
- `frame restore`.
- Development URL detection in production output.
- Missing asset and unresolved alias checks.
- Generated Liquid size checks.
- Ownership-ledger validation.
- Recovery from interrupted development sessions.

### Phase 4: hardening and public beta

**Duration:** approximately 2 weeks

Deliver:

- Full operating-system and Node/Vite CI matrix.
- Package provenance and trusted publishing.
- Migration guide from Barrel Shopify Vite.
- Skeleton and Dawn examples.
- Tailwind and Alpine recipes.
- GitHub Actions package and distribution-branch recipes.
- Shopify CLI push/package documentation.
- Compatibility policy.
- Troubleshooting guides for CORS, Theme Editor behavior, static assets, and generated output.

### Post-MVP candidates

Only pursue these after validating demand:

- Automatic entry discovery with inspectable output.
- Per-entry generated snippets.
- Section-folder compilation.
- Theme Editor lifecycle helper.
- Import-map plugin.
- Islands plugin.
- Bundle analysis and performance budgets.
- Optional managed tunnel integration.
- Migration codemods.

## Success criteria

### Adoption

- A developer can add Frame to an existing Shopify theme in under ten minutes.
- No mandatory source restructuring.
- Migration from Barrel Shopify Vite has a documented mechanical path.

### Correctness

- No stale Frame assets after an entry or chunk is removed.
- No user-owned asset is deleted.
- No development URL appears in production output.
- Multiple entries build without excessive generated Liquid.
- CSS order and shared chunks are deterministic.

### Developer experience

- Frame and Shopify CLI remain independently runnable and composable.
- JavaScript and CSS use normal Vite HMR.
- With the optional notification handshake, Liquid reload occurs only after Shopify CLI reports idle.
- One completed theme update produces one browser reload when notification coordination is enabled.
- Diagnostic output identifies paths, entries, ownership, origin choices, and notification configuration.

### Maintainability

- Small dependency tree.
- No custom compiler.
- No production runtime.
- Public manifest schema is versioned.
- Current and previous supported Vite majors are tested.
- Linux, macOS, and Windows are tested.

## Key product decisions to validate

### 1. Stable versus hashed entry filenames

**Recommendation:** stable top-level entry names and content-hashed shared/dynamic chunks.

Validate Shopify cache behavior using `asset_url` version parameters and a real store before finalizing.

### 2. Single dispatcher versus per-entry Liquid snippets

**Recommendation:** begin with a compact dispatcher over explicit entries and enforce a conservative size threshold. Add per-entry snippets only if real projects require them.

### 3. Repository versus pipeline generated-output mode

**Recommendation:** make pipeline mode the cleanest documented workflow, with CI producing a deployable artifact or distribution branch. Support repository mode explicitly without hidden Git index manipulation.

### 4. Frame CLI scope

**Decision:** Vite owns development and builds through `vite` and `vite build`. Frame-specific commands are limited to inspect, doctor, clean, and restore. Frame will not launch or wrap any Shopify CLI command. Users run `shopify theme dev`, `shopify theme push`, and `shopify theme package` directly.

### 5. Vite manifest versus direct bundle metadata

**Recommendation:** use Vite’s official manifest internally for entry relationships and produce a versioned Frame manifest. Avoid making undocumented Vite metadata the public contract.

### 6. Tunnel support

**Recommendation:** initially accept an explicit external origin and document external tunnel tools. Add managed tunnel providers only if local-network browser restrictions make them necessary for most users.

### 7. Theme Editor runtime helper

**Recommendation:** documentation and recipes first. Keep the core runtime-free. Add an optional lifecycle helper only after confirming repeated demand.

## Recommended differentiators

If Frame executes only five ideas exceptionally well, they should be:

1. Optional Shopify CLI `--notify`-driven reload coordination without process coupling.
2. Explicit multi-entry Vite builds with stable entries and hashed chunks.
3. A generated-file ownership ledger with post-success cleanup only.
4. Inspectable configuration and actionable diagnostics.
5. No frontend framework, starter-theme, Git-policy, or storefront-runtime opinions.

## Brand direction

**Name:** Frame

**Family:** Blueprint

Possible product expressions:

- Blueprint Frame
- Frame by Blueprint
- `@blueprint/frame`
- `frame dev`
- `frame build`
- `frame inspect`

Possible taglines:

- Frame your theme for production.
- The build system for Blueprint themes.
- Build the structure behind your Shopify theme.
- Blueprint defines it. Frame builds it.
- A stronger frame for Shopify Liquid development.

## Primary sources

### Shopify

- Theme build tools: <https://shopify.dev/docs/storefronts/themes/tools>
- Shopify CLI for themes: <https://shopify.dev/docs/storefronts/themes/tools/cli>
- File transformation guidance: <https://shopify.dev/docs/storefronts/themes/best-practices/file-transformation>
- Theme architecture: <https://shopify.dev/docs/storefronts/themes/architecture>
- Theme Check: <https://shopify.dev/docs/storefronts/themes/tools/theme-check>
- Shopify CLI repository: <https://github.com/Shopify/cli>
- Theme tools repository: <https://github.com/Shopify/theme-tools>
- Dawn: <https://github.com/Shopify/dawn>
- Skeleton Theme: <https://github.com/Shopify/skeleton-theme>

### Direct build-tool competitors

- Barrel Shopify Vite: <https://github.com/barrel/shopify-vite>
- Barrel documentation: <https://shopify-vite.barrelny.com/>
- `woodawn/vite-plugin-shopify-theme`: <https://github.com/woodawn/vite-plugin-shopify-theme>
- Syncify: <https://github.com/panoply/syncify>
- Melter: <https://github.com/unshopable/melter>

### Starters and development environments

- Shopify Theme Lab: <https://github.com/uicrooks/shopify-theme-lab>
- Adastra: <https://github.com/odestry/adastra>
- Slayed: <https://github.com/james0r/slayed>
- Shopify Theme Tailwind: <https://github.com/wesselvanree/shopify-theme-tailwind>
- Trellis Dawn and Tailwind starter: <https://github.com/TrellisCommerce/shopify-tailwind-starter-base>
- Slatest: <https://github.com/entozoon/slatest>

### Specialized packages

- Import maps: <https://github.com/slavafyi/vite-plugin-shopify-import-maps>
- Shopify theme islands: <https://github.com/Rees1993/vite-plugin-shopify-theme-islands>
- Liquid Ajax Cart: <https://github.com/EvgeniyMukhamedjanov/liquid-ajax-cart>
- Theme development tools: <https://github.com/yakohere/shopify-theme-devtools>
