# Easel + Vue

A minimal Shopify Liquid theme using Easel, Vue 3, Vue single-file components, and the official Vue Vite plugin.

Vue mounts only where Liquid renders `[data-vue-counter]` and remounts sections replaced by the Shopify Theme Editor.

```sh
npm install
npm run dev
shopify theme dev --notify .easel/shopify-ready
```
